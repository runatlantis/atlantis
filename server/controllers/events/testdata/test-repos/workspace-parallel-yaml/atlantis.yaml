version: 3
parallel_plan: false
parallel_apply: true
projects:
  - dir: .
    workspace: production
    autoplan:
      when_modified: ["**/*.tf*"]
  - dir: .
    workspace: staging
    autoplan:
      when_modified: ["**/*.tf*"]
