# Block malicious third-party git paths
allow-git=none

# Block lifecycle script detonation
ignore-scripts=true

# Enforce exact local environment configurations
engine-strict=true

# Fail if peer dependencies conflict unexpectedly
legacy-peer-deps=false
